services:
  merlins-ide:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: merlins-ide
    restart: unless-stopped
    ports:
      - "19700:19700"
    volumes:
      - ide-data:/data
    environment:
      - NODE_ENV=production
      - PORT=19700
      - HOST=0.0.0.0
      - DB_PATH=/data/the-ide.db
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
      - CORS_ORIGINS=${CORS_ORIGINS:-*}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:19700/api/v1/health"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

volumes:
  ide-data:
    driver: local
